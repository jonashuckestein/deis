[Unit]
Description=Weekly deis-builder cleanup

[Timer]
# see systemd.time(7) manual page for other scheduling options
OnCalendar=weekly
# run immediately if we missed a backup for some reason
Persistent=true

[Install]
WantedBy=timers.target

[X-Fleet]
X-ConditionMachineOf=deis-builder.service
